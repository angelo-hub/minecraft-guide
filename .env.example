# Copy this file to .env and fill in values.
# .env is gitignored — never commit it.

# Port the guide web server listens on
PORT=3000

# Minecraft RCON (leave RCON_PASSWORD empty to disable TPS stats)
RCON_PORT=25575
RCON_PASSWORD=
